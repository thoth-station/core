backend_namespace: "thoth-test-core"
middletier_namespace: "thoth-test-core"
frontend_namespace: "thoth-test-core"
infra_namespace: "thoth-test-core"
amun_api_namespace: "thoth-test-core"
amun_inspection_namespace: "thoth-amun-inspection"
deployment_name: "thoth-test-core"
ceph_secret_key: "{{ vault_ceph_secret_key }}"
ceph_key_id: "{{ vault_ceph_key_id }}"
s3_endpoint_url : "{{ vault_s3_endpoint_url }}"
ceph_bucket: "DH-DEV-DATA"
app_secret: "{{ vault_secret }}"
sentry_dsn: "{{ vault_sentry_dsn }}"
prometheus_pushgateway_host: "{{ vault_prometheus_pushgateway_host }}"
prometheus_pushgateway_port: "{{ vault_prometheus_pushgateway_port }}"
zuul_incoming_webhook_secret: "{{ vault_zuul_incoming_webhook_secret }}"
amun_app_secret: "{{ vault_secret }}"
amun_api_url: "http://amun-api-{{ frontend_namespace }}.cloud.paas.upshift.redhat.com/api/v1"
thoth_user_api_host: "user-api-{{ frontend_namespace }}.cloud.paas.upshift.redhat.com"
